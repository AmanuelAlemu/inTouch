<!-- Scroll view gives us the scroll bar when we get the search results. Scrolling isn't included by default for some reason.
     At the end of LinearLayout, divider is the line between all the contacts. I set it equal to the divider file
     in drawable. Not sure which drawable my phone uses, so I stuck it in all of them. Which I think I need to do anyway
     for phones of all sizes to get the divider I don't think the two android_layoutMarginTop/Bottom lines do anything, that's why
     I set size in the Java file. I think it should work from here too, but idk how. -->

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ScrollView01"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >


	<RelativeLayout 
    	
    	android:layout_width="match_parent"
    	android:layout_height="wrap_content"
    	android:paddingBottom="@dimen/activity_vertical_margin"
    	android:paddingLeft="@dimen/activity_horizontal_margin"
    	android:paddingRight="@dimen/activity_horizontal_margin"
    	android:paddingTop="@dimen/activity_vertical_margin"
    	>
	
    
    	<LinearLayout
        	android:id="@+id/info"
        	android:layout_width="match_parent"
        	android:layout_height="wrap_content"
        	android:text="@string/hello_world"
        	android:layout_marginTop="6dip"
        	android:layout_marginBottom="6dip"
        	android:orientation="vertical"
        	android:divider="@drawable/divider"
        	android:showDividers="middle"
        	/>
    
	</RelativeLayout>

</ScrollView>